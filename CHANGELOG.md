# Test
## 🚀 新功能 (New Features)

* **多地图支持**: 新增了对 **高德地图** 和 **百度地图** 的支持，现在可以在设置中自由切换数据源。
* **多语言适配**: 彻底重构了语言模块，新增支持 **德语 (Deutsch)** 和 **韩语 (한국어)**。
* **UI 优化**: 搜索栏左侧图标增加了淡灰色底色，视觉效果更加统一。
* **自动构建**: 迁移至 GitHub Actions 自动化发布流程。

## 🐛 问题修复 (Bug Fixes)

* 修复了在 Android 12+ 设备上动态取色失效的问题。
* 修复了繁体中文环境下，API Key 弹窗显示乱码的 Bug。
* 解决了离线地图下载时，进度条偶尔卡在 99% 的问题。

## 🛠️ 底层改进 (Under the Hood)

* 升级 Flutter SDK 至 `3.38.4`。
* 优化了 `Build & Release` 工作流，不再上传多余的 Artifacts。
* 重构了 API 搜索逻辑，引入策略模式 (Strategy Pattern) 以支持更多地图源。

---

> 此版本由 GitHub Actions 自动构建生成。
> SHA: ${{ github.sha }}